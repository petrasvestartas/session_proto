syntax = "proto3";

package session_proto;

// Xform message representing a 4x4 transformation matrix
message Xform {
  string guid = 1;             // Unique identifier
  string name = 2;             // Transformation name
  repeated double matrix = 3;  // 16 values in column-major order
}
